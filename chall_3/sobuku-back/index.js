const { levels } = require("./modules/_create.levels");

const onReordone = (
    { 
        options: {
            level,
            comp
        } 
    }
) => {

    const m = level && level['dimensionsMatrice'];
    const n = level && level['dimensionsMatrice'];
    const vercteurs = [];

    for(let i; i < m; i++){
        for(let k; k < n; k++){
            
        }
    }
}

onReordone({
    options: {
        level: levels['_level_1'],
        comp: {}
    }
})

